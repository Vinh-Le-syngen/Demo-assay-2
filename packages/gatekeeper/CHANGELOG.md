# Changelog — @sys/gatekeeper

## 0.0.1

Initial extraction from Qarar (2026-06-03). Promotion gate — a pure ALLOW/DENY/REVIEW verdict for branch promotions (feature→staging→main) with tests/gates/risk/human-approval policy. Library or the `sys-gatekeeper` CLI.
