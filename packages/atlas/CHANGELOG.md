# Changelog — @sys/atlas

## 0.0.1

Initial extraction from Qarar (2026-06-03). Dependency-graph impact analysis + propagation guard. Pure engine over a declared file dependency graph; usable as a library or the `sys-atlas` CLI.
