# Changelog — @sys/herald

## 0.0.1

Initial extraction from Qarar (2026-06-03). Notification decision + delivery-harness subsystem. Pure NotificationPolicyEngine (intent + recipient + policy + consent → message plan) + suppression model + idempotency/replay contracts + channel-adapter interfaces. No I/O, no credentials, no host vocabulary.
