// @sys/canon — Governed-framework RECORDS (Data + Governance planes).
// Schemas (the cross-language contract), provenance, validation, policy data, and JSON-Schema emission.
// Decisions live in @eng/governance (systems own truth; engines decide).
export * from './core'
export * from './seo'
export * from './schema-export'
