# Changelog — @sys/canon

## 1.0.0

Promoted to `1.0.0` on adopting Changesets + real SemVer (2026-06-08). `@sys/canon` is a
load-bearing, consumed system and an internal-dependency target (`@eng/governance` depends on
it), so it leaves `0.x`. No API change — a versioning-policy promotion. Captured in the
`baseline@2026.06.0` release set.

## 0.0.1

Initial extraction from Qarar (2026-06-03). Governed-framework RECORDS: config schemas (cross-language contract) + provenance + validation + policy data. Systems own truth; decisions live in @eng/governance. Configurable, cross-project, 5-plane-aligned.
