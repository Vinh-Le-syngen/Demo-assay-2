# Changelog — @sys/vault

## 0.0.1

Initial extraction from Qarar (2026-06-03). Jurisdiction-aware document vault: versioned evidence + metadata + access + audit + retention policy. Owns the domain; storage/store/identity injected. 5-plane-aligned.
