// @sys/vault — root entry. Document-side domain (v1): the vault model, the default access policy,
// reuse logic, and the createVault orchestrator. Storage/store/identity are injected by the host.
export * from './core/index'
export * from './config'
